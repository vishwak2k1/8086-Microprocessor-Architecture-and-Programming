; COMPUTE FACTORIAL and STORE THE RESULT in LOCATION 1000h
; n! = (n*n-1) * (n-2) * .. 1

MOV BL,  5
MOV AL, 1; RESULT

BACK:
MUL BL
DEC BL
JNZ BACK
MOV [1000H], AX ; THE RESULT WILL BE STORED IN AX, 
